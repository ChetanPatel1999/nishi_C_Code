#include <stdio.h>
void main()
{
    // 12 > 7 ? printf("hello") : printf("by");
    // 12 > 77 ? printf("hello") : printf("by");
    // 12 > 77 && 3 == 5 ? printf("hello") : printf("by");
    // 1 ? printf("hello") : printf("by");
    0 ? printf("hello") : printf("by");
}